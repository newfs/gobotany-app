Genus:
    {{ object.name }}.

{# Common name is not currently planned for genera, but include it if it exists. #}
{% if object.common_name %}
    Common name:
        {{ object.common_name }}.
{% endif %}

Family:
    {{ object.family.name }}.

Description:
    {{ object.description }}

Species:
{% for species in object.taxa.all %}
    {{ species.genus|make_list|first }}. {{ species.epithet }}
{% endfor %}
